import React, { FC } from "react"
import { FooterWrapper } from "./footer.styled";

const Footer:FC = () => {
    return (
        <FooterWrapper>
            FOOTER
        </FooterWrapper>
    )
}

export default Footer;